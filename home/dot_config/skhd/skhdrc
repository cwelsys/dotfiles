# change window focus within space
alt - j: yabai -m window --focus south
alt - k: yabai -m window --focus north
alt - h: yabai -m window --focus west
alt - l: yabai -m window --focus east

# change focus between external displays (left and right)
alt - s: yabai -m display --focus west
alt - g: yabai -m display --focus east

# rotate layout clockwise
shift + alt - r: yabai -m space --rotate 270

# flip along y-axis
shift + alt - y: yabai -m space --mirror y-axis

# flip along x-axis
shift + alt - x: yabai -m space --mirror x-axis

# toggle window float
shift + alt - t: yabai -m window --toggle float --grid 4:4:1:1:2:2

# maximize a window
shift + alt - m: yabai -m window --toggle zoom-fullscreen

# balance out tree of windows (resize to occupy same area)
shift + alt - e: yabai -m space --balance

# swap windows
shift + alt - j: yabai -m window --swap south
shift + alt - k: yabai -m window --swap north
shift + alt - h: yabai -m window --swap west
shift + alt - l: yabai -m window --swap east

# move window and split
ctrl + alt - j: yabai -m window --warp south
ctrl + alt - k: yabai -m window --warp north
ctrl + alt - h: yabai -m window --warp west
ctrl + alt - l: yabai -m window --warp east

# move window to display left and right
shift + alt - s: yabai -m window --display west; yabai -m display --focus west
shift + alt - g: yabai -m window --display east; yabai -m display --focus east

# move window to prev and next space
shift + alt - p: yabai -m window --space prev
shift + alt - n: yabai -m window --space next

# move window to space #
shift + alt - 1: yabai -m window --space 1
shift + alt - 2: yabai -m window --space 2
shift + alt - 3: yabai -m window --space 3
shift + alt - 4: yabai -m window --space 4
shift + alt - 5: yabai -m window --space 5
shift + alt - 6: yabai -m window --space 6
shift + alt - 7: yabai -m window --space 7
shift + alt - 8: yabai -m window --space 8

# stop/start/restart yabai
ctrl + alt - q: yabai --stop-service
ctrl + alt - s: yabai --start-service
ctrl + alt - r: yabai --restart-service

# ADDED FROM WHKDRC
# Reload configuration
alt + shift - o: yabai --restart-service

# Close window (Alt+Q)
alt - q: yabai -m window --close

# Minimize window (Alt+M)
alt - m: yabai -m window --minimize

# Cycle focus through windows
alt + shift - bracketleft: yabai -m window --focus prev
alt + shift - bracketright: yabai -m window --focus next

# Promote window to the top of the tree (similar to komorebi promote)
shift + alt - return: yabai -m window --swap first

# Stack windows
alt - left: yabai -m window --stack west
alt - down: yabai -m window --stack south
alt - up: yabai -m window --stack north
alt - right: yabai -m window --stack east

# Unstack
alt - semicolon: yabai -m window --toggle stack

# Cycle through stack
alt - bracketleft: yabai -m window --focus stack.prev
alt - bracketright: yabai -m window --focus stack.next

# Resize windows
alt - minus: yabai -m window --resize left:-20:0 || yabai -m window --resize right:-20:0
alt - equals: yabai -m window --resize left:20:0 || yabai -m window --resize right:20:0
alt + shift - minus: yabai -m window --resize bottom:0:-20 || yabai -m window --resize top:0:-20
alt + shift - equals: yabai -m window --resize bottom:0:20 || yabai -m window --resize top:0:20

# Focus spaces directly (Alt + number)
alt - 1: yabai -m space --focus 1
alt - 2: yabai -m space --focus 2
alt - 3: yabai -m space --focus 3
alt - 4: yabai -m space --focus 4
alt - 5: yabai -m space --focus 5
alt - 6: yabai -m space --focus 6
alt - 7: yabai -m space --focus 7
alt - 8: yabai -m space --focus 8
