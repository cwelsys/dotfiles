{
	"custom/weather": {
		"exec-if": "which wttrbar",
		"exec": "wttrbar --fahrenheit",
		"restart-interval": 300,
		"tooltip": true,
		"format": "{}°",
		"return-type": "json",
		"on-click": "kitty --hold --class center-float fish -c 'wttr -u'",
		"on-click-right": "kitty --hold --class center-float fish -c 'wttr -u -v2'",
		"on-click-middle": "xdg-open https://wttr.in/"
	}
}
