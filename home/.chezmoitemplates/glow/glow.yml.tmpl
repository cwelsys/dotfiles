{{ $themePath = printf "%s/.config/glow/themes/catppuccin-%s.json" .chezmoi.homeDir .catppuccinFlavor }}
# style name or JSON path (default "auto")
style: {{ $themePath }}
# show local files only; no network (TUI-mode only)
local: true
# mouse support (TUI-mode only)
mouse: true
# use pager to display markdown
pager: true
# word-wrap at width
width: 80
